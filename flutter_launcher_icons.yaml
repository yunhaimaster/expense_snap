# flutter_launcher_icons 配置
# 執行: flutter pub run flutter_launcher_icons

flutter_launcher_icons:
  android: true
  ios: false  # 暫時只做 Android

  # 主 App Icon
  image_path: "assets/icon/App_Icon.png"

  # 移除 alpha channel（避免透明問題）
  remove_alpha_ios: true
